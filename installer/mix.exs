defmodule Agala.New.Mixfile do
  use Mix.Project

  def project do
    [app: :agala_new,
     version: "2.0.0",
     elixir: "~> 1.4"
    ]
  end

  def application do
    [applications: []]
  end
end
